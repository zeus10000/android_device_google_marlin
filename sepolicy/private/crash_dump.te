# Marlin-specific crash_dump permissions
#
# Android 16 enforces a neverallow preventing crash_dump from ptracing
# keystore (it would leak cryptographic key material into tombstones).
# Keystore crashes are instead handled via partial tombstones without
# ptrace details, per the upstream security model.
#
# This file is reserved for marlin-specific supplementary rules.
