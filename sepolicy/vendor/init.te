# init needs to mount /persist and relabel debugfs on msm8996
allow init persist_file:dir mounton;
allow init debugfs_ipc:{ dir file } relabelfrom;
allow init debugfs_usb:{ dir file } relabelfrom;
allow init sysfs_scsi_devices_0000:file write;
